[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "leuco-live-demo"
version = "0.1.0"
description = "Host-native Leuco live drowning-risk demo for Jetson"
requires-python = ">=3.10"
dependencies = []

[tool.setuptools.packages.find]
where = ["src"]
